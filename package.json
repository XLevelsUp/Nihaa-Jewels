{
  "name": "nihaa-jewels",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:marketing": "npm run dev --workspace=marketing",
    "dev:admin": "npm run dev --workspace=admin",
    "build:marketing": "npm run build --workspace=marketing",
    "build:admin": "npm run build --workspace=admin",
    "start:marketing": "npm run start --workspace=marketing",
    "start:admin": "npm run start --workspace=admin",
    "lint:marketing": "npm run lint --workspace=marketing",
    "lint:admin": "npm run lint --workspace=admin"
  }
}
